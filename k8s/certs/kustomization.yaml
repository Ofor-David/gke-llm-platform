apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - linkerd-trust-root-issuer.yaml
  - trust-anchor-certificate.yaml
  - linkerd-identity-issuer.yaml
  - linkerd-identity-issuer-certificate.yaml
  - trust-bundle.yaml
  - webhook-certificates.yaml